[project]
name = "socratic-rl"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["openenv-core", "fastapi", "uvicorn"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
